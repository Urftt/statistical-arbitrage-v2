# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Frontend framework | Next.js (React) replacing Dash | Dash callback model caused jank; React gives full UX control needed for guided Academy experience | No |
| D002 | M001 | arch | Backend framework | FastAPI wrapping existing Python analysis | analysis/research.py already has zero Dash imports; natural separation. FastAPI adds OpenAPI docs, async, Pydantic validation | No |
| D003 | M001 | arch | Frontend-backend communication | REST API (FastAPI → Next.js) | Simple, well-understood, sufficient for the workload. No need for GraphQL or WebSockets yet | Yes — if real-time trading needs streaming |
| D004 | M001 | data | Data pipeline | Keep existing CCXT/Bitvavo → parquet cache as-is | Working pipeline with months of data. API layer wraps it, doesn't replace it | No |
| D005 | M001 | scope | Exchange support | Bitvavo only, no multi-exchange abstraction | User preference. Keeps scope focused. Revisit only if trading scope expands | Yes — if user wants other exchanges |
| D006 | M001 | convention | DataFrame library | Polars (not Pandas) for all dataframe operations | Existing codebase convention. Polars is faster and more explicit | No |
| D007 | M001 | arch | Chart library | Plotly via react-plotly.js | Existing chart code produces Plotly figures. react-plotly.js renders them natively in React | No |
| D008 | M001 | scope | Deployment target | Local development only (laptop) | Server deployment deferred until live trading proves viable | Yes — migrate to home server for M004 |
| D009 | M001 | convention | Platform transparency | Every formula, assumption, and decision visible to user | Core value anchor. Applies to Academy, Research, and Backtesting equally. Platform must never feel like a black box | No |
| D010 | M001/S03 | arch | Academy step rendering architecture | Standalone 'use client' step components dispatched by activeStep index in page.tsx, with data fetched at page level and passed as props | Separates data fetching from rendering. Page-level fetch + cache (useRef keyed by asset1-asset2-timeframe) avoids redundant API calls when navigating between steps | No |
| D011 | M001/S03 | convention | Plotly subplot implementation in react-plotly.js | Manual dual-axis positioning (xaxis/xaxis2 with domain splits) instead of make_subplots | react-plotly.js has no subplot helper like Python's make_subplots. Manual domain positioning gives full control. Must manually apply dark theme to xaxis2/yaxis2 | No |
| D012 | M001/S03 | convention | SSR-safe synthetic chart data | Deterministic seededRandom() PRNG instead of Math.random() | Math.random() produces different values on server vs client, causing React hydration mismatches. Seeded PRNG ensures identical output on both render passes | No |
| D013 | M001/S05 | frontend-performance | How Scanner executes large batch cointegration scans | Run pair scans in chunks of 5 concurrent requests with progress tracking | Chunked concurrency avoids overwhelming the local FastAPI backend while still giving responsive progress feedback and predictable failure handling. | Yes |
| D014 | M001/S05 | frontend-data | How Deep Dive derives configurable z-scores | Recompute z-scores client-side from the returned spread and keep OHLCV fetches aligned with the selected history window | The cointegration endpoint hardcodes a 60-period z-score, so client-side recomputation preserves the page's z-score-window control without changing the backend contract. | Yes |
| D015 | M001/S06/T03 | frontend-shell | How to handle the Mantine-generated hydration mismatch that appeared during final dev-stack UAT. | Render the dashboard AppShell only after mount in the client layout. | The final live pass surfaced SSR/client id and classname mismatches from Mantine layout primitives. Mount-gating the dashboard shell removed the mismatch cleanly while preserving the real runtime behavior needed for local UAT and keeping the static gate green. | Yes |
| D016 | M002 planning | execution-order | How M002 should be sliced and executed | Prove the milestone through a real connected research→backtest flow first, then expand to the remaining research breadth, then add grid search/walk-forward robustness features, and finish with explicit live integration closure. | The backtesting engine and research handoff are the main technical and product risks, while the remaining research-module wrappers are comparatively mechanical. Proving the risky path early creates stable contracts for later breadth and prevents a late-stage integration surprise. | Yes |
| D017 | M002 planning | verification | What counts as completion proof for M002 | Treat M002 as incomplete until the assembled localhost flow Research Hub → recommended parameters → Backtester → metrics/warnings/footer is exercised live through the real Next.js and FastAPI entrypoints. | This milestone spans a pure-Python engine, API contracts, and React pages. Per-surface tests are necessary but insufficient; the platform's transparency promise only holds when the connected runtime behavior is verified end-to-end. | No |
| D018 | M002 S01 T01 | backtesting | Look-ahead-safe spread and execution model for the first backtest core | Use trailing-window OLS hedge ratios/z-scores computed only from data available through each signal bar, and execute signals on the next bar's close with fee-aware pair notional accounting. | A full-sample hedge ratio would leak future information into signal generation. The project currently has close-only candle data, so the most honest deterministic execution model is next-bar close fills plus an explicit footer that states the close-only/slippage limitations. | Yes |
| D019 | M002 S01 T02 | api-contract | How the first research-to-backtest API boundary should be shaped | Expose a shared FastAPI contract where the research lookback sweep returns a fully valid nested BacktestRequest (`recommended_backtest_params`) and the backtest endpoint always returns structured `data_quality`, `warnings`, and `footer` metadata instead of surfacing blockers as 500s. | A directly postable recommendation payload removes adapter glue between the research and backtest surfaces, while structured blocker/warning/footer fields preserve the platform's transparency promise and give S02/S03 stable envelope fields to extend. | Yes |
| D020 | M002/S01/T03 | frontend-routing | How the first research-to-backtest handoff should preserve and transport recommended settings. | Encode the full recommended backtest preset in `/backtest` URL search params and let the page hydrate controls from that URL. | The handoff needs to be shareable, refresh-safe, and directly inspectable in both browser verification and future debugging. Explicit URL params keep the contract stateless and visible, avoid hidden client-side storage, and let reruns update the same durable preset representation. | Yes |
| D021 | M002/S02/T01 | api-contract | How z-score is computed for research endpoints that need it (z-score threshold, tx-cost) | Pre-compute z-score via Polars rolling_mean/rolling_std in a shared _compute_zscore helper inside research.py, rather than calling PairAnalysis.calculate_zscore or the /api/analysis/zscore endpoint. | The research endpoints need z-score as an intermediate for threshold sweeping and cost analysis but don't need the full PairAnalysis lifecycle. A lightweight Polars-native helper keeps the computation in the router without adding a dependency on the analysis endpoint or constructing a PairAnalysis instance. | Yes |
| D022 | M002/S02/T02 | frontend-performance | How the 8-module Research Hub organizes and loads panel components | Mantine Tabs with keepMounted={false} and next/dynamic lazy loading per panel. Each tab's panel component is dynamically imported with { ssr: false } and a loading skeleton. Only the active tab mounts its panel. | Loading all 8 research panels simultaneously would import Plotly 8 times and mount unused chart components. keepMounted={false} ensures only the visible panel is in the DOM, and next/dynamic avoids SSR crashes from Plotly's window access. A RESEARCH_MODULES constant array with lazy Panel references keeps the page.tsx module picker clean and extensible. | Yes |
